{
  "version": 2,
  "buildCommand": "npm run build",
  "routes": [{ "src": "/(.*)", "dest": "api/index.ts" }],
  "crons": [{ "path": "/internal/cron/ibim", "schedule": "0 0 * * *" }],
  "functions": {
    "api/index.ts": { "maxDuration": 60 }
  }
}
